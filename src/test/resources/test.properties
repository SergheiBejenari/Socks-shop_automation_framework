# Test properties file for testing variable expansion and properties loading

# Basic test values
testKey=testValue
testInt=42
testBoolean=true

# Variable expansion tests
baseUrl=http://localhost:8080
apiPath=/api/v1
fullApiUrl=${baseUrl}${apiPath}

# Nested expansion
protocol=http
host=localhost
port=8080
baseUrlExpanded=${protocol}://${host}:${port}

# Test with spaces and special characters
messageTemplate=Hello ${userName}, welcome to ${appName}!
userName=TestUser
appName=Socks Shop

# Circular reference test (should be detected)
# circularA=${circularB}
# circularB=${circularA}

# Environment variable style
DATABASE_URL=jdbc:postgresql://localhost:5432/testdb
REDIS_URL=redis://localhost:6379/0
