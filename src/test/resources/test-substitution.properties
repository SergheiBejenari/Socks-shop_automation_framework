# Test properties file for variable substitution
baseUrl=https://example.com
apiUrl=${baseUrl}/api
fullApiUrl=${apiUrl}/v1
nestedVar=${fullApiUrl}/users
mixedVar=prefix-${baseUrl}-suffix

# Test undefined variable (should remain unexpanded)
undefinedVar=${UNDEFINED_VAR}
